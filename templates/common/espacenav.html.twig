<div id="espacemainnav">
    <div class="mb-3">
        <i class="fas fa-arrow-left pointer closeespacemainnav"></i>
    </div>
    <h4 class="text-center mb-5">Zhen-Ecommerce</h4>
    <div class="onemenu {% if app.session.get("nav")=='profile' %}active{% endif %}">
        <a class="" href="{{ path('profilepage') }}">Mon profil</a>
    </div>
    <div class="onemenu {% if app.session.get("nav")=='commands' %}active{% endif %}">
        <a class="" href="{{ path('mycommandspage') }}">Mes commandes</a>
    </div>
    <div class="onemenu {% if app.session.get("nav")=='mycomments' %}active{% endif %}">
        <a class="" href="{{ path('mycommentspage') }}">Mes commentaires</a>
    </div>
    <div class="onemenu">
        <a class="" href="#">Mes messages</a>
    </div>
    <div class="onemenu">
        <a class="" href="#">Mes newsletters</a>
    </div>

     {% if is_granted('ROLE_ADMIN') %}
        <div class="text-center mb-3 mt-5 h5 font-weight-bold">Admin</div>
        <div class="onemenu {% if app.session.get("nav")=='adminallusers' %}active{% endif %}">
            <a class="" href="{{ path('adminallusers') }}">
            <i class="fas fa-users"></i>
             Users
            </a>
        </div>
        <div class="onemenu {% if app.session.get("nav")=='adminallcategories' %}active{% endif %}">
            <a class="" href="#">
            <i class="fab fa-cuttlefish"></i>
             Categories
            </a>
        </div>
        <div class="onemenu {% if app.session.get("nav")=='adminallproducts' %}active{% endif %}">
            <a class="" href="{{ path('adminallproducts') }}">
            <i class="fas fa-database"></i> 
             Articles
            </a>
        </div>
        <div class="onemenu {% if app.session.get("nav")=='adminallcommands' %}active{% endif %}">
            <a class="" href="{{ path('adminallcommands') }}">
            <i class="fas fa-euro-sign"></i>
             Commands
            </a>
        </div>
        <div class="onemenu ">
            <button type="button" class="btn btn-outline-primary btn-sm testemail">Test E-mail</buttou>
        </div>
    {% endif %}
</div>